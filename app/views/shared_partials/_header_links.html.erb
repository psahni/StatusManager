<% if member_signed_in? %>
    <div class="pull-right top-links">
        <ul>
            <li><a href="#" class="special-link"><%=current_member.name%></a></li>
            <% if current_member.super_admin? or current_member.admin? %>
                <li><%=link_to "Settings", settings_path%></li>
                <li><a href="">Track Status</a></li>
            <% else %>
                <li><%=link_to "Track your status", track_status_status_index_path%></li>
            <% end %>
            <li><a href="#">Logout</a></li>
        </ul>
    </div>
<% end %>
